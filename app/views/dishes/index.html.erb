<h2>All dishes</h2>

<%=link_to 'add', new_dish_path(@user_id)%>
  <%@dishes.each do |d|%>
    <%=d.name%>
    <br>
    <div class="col-xs-12">
    <%d.photos.each do |p|%>
      <div class="index">

    <!-- <div class="see"> -->
      <!-- <%#=link_to p.user.username, user_path(p.user)%> took this photo at <%#=p.venue.venue_name%> -->
      <%= link_to (cl_image_tag p.image), dish_path(d.id) %>
    </div>

    <%end%>
    </div>
    <%end%>
