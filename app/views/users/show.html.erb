<h2><%=@user.username%>'s details</h2>


<%if @current_user == @user%>
  <%=link_to 'edit profile',edit_user_path(@user)%>
<%end%>
<br>

<div class="show">
  <%= cl_image_tag @user.image, :class => 'img-rounded face' %>
</div>

  <p>Username: <%=@user.username%></p>
  <p>Full name: <%=@user.full_name%></p>
  <p>Email: <%=@user.email%></p>
  <p>Instagram: <%=@user.instagram%></p>
  <p>About me: <%=@user.bio%></p>
  <p>Website: <%=@user.website%></p>

Favourites:
<div class='row'>

<%@user.favorites.each do |f|%>
  <div class="col s1">

  <%= link_to '<i class="small material-icons">star</i>'.html_safe, photo_path(f.favorited_id)%>
</div>
<%end%>
</div>


<h4><%=@user.username%>'s photos</h4>
<%=link_to 'add a dish', new_dish_path(@user_id)%>
<ul>
  <%@user.photos.each do |p|%>
    <div class="index">
    <%= link_to (cl_image_tag p.image), photo_path(p.id), :class => 'img-thumbnail' %>
    </div>
  <%end%>

</ul>
