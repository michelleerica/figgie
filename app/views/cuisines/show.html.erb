<h2><%=@cuisine.category%></h2>

ID: <%=@cuisine.id%>


<ul>
  <%@cuisine.photos.each do |p|%>
  <%= link_to (image_tag p.image), photo_path(p.id), :class => 'post_preview' %>
  <br>

  <%= p.user.username %> said: <%= p.description %>
  <br>


<%end%>

</ul>
