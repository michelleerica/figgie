<!DOCTYPE html>
<html>
  <head>
    <title>Figgie</title>
    <%= csrf_meta_tags %>
    <%=cloudinary_js_config%>



  <!-- Compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.99.0/css/materialize.min.css">

  <!-- Compiled and minified JavaScript -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.99.0/js/materialize.min.js"></script>
  <!--Import Google Icon Font-->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

  </head>

  <body>

    <nav>
      <div class="nav-wrapper #00838f cyan darken-3">

        <%=link_to 'figgie', root_path, :class => "brand-logo center" %>
        <ul id="nav-mobile" class="left hide-on-med-and-down">

          <li><%=link_to 'venues', venues_path %></li>
          <li><%=link_to 'friends', users_path %></li>
          <li>  <%=link_to 'cuisines', cuisines_path %></li>
          <li><%=link_to 'categories', categories_path %></li>
        </ul>
        <ul id="nav-mobile" class="right hide-on-med-and-down">

        <% unless @current_user.present?%>
          <li><%=link_to 'Login', login_path %>  </li>
          <li><%=link_to 'Sign up', new_user_path%></li>
        <%else%>
          <li><%= link_to 'Logout', login_path, method: :delete%></li>
          <li><%= link_to (cl_image_tag @current_user.image, :id => 'icon'), user_path(@current_user)%></li>
          <li><%=link_to '+ ', new_dish_path %></li>
        <!-- </ul> -->
        <%end%>
      </ul>
      </div>
    </nav>




      </div>
    </div>
    <div class="container-fluid">
      <% unless @current_user.present?%>
        <%=link_to 'Login', login_path %> | <%=link_to 'Sign up', new_user_path%>
      <%else%>
        Welcome, <%=link_to @current_user.username, user_path(@current_user)%>
      <%end%>
    </div>

    <div class="container">
      <%= yield %>
    </div>

  </body>
</html>
